# Filter a Network Pipeline

Filter a Network Pipeline

## Usage

``` r
where(query_list, ...)
```

## Arguments

- query_list:

  A xafty query obtained via \[\`xafty::query\`\]

- ...:

  Filter conditions, e.g. \`col1 \>= 1“\`

## Value

A xafty query with a where filter applied
