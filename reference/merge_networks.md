# Merge networks into one

The function allows the merging of any numbers of networks into one
network. The return value is therefore always one network.

## Usage

``` r
merge_networks(name, ...)
```

## Arguments

- name:

  Character vector of length 1. Name of the merged network

- ...:

  Networks that should be merged

## Value

A 'xafty_network' environment
